{% extends 'base.html.twig' %}

{% block title %}Artist{% endblock %}

{% block body %}
    <h1>Artist</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ artist.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ artist.name }}</td>
            </tr>
            <tr>
                <th>Country</th>
                <td>{{ artist.country }}</td>
            </tr>
            <tr>
                <th>Style</th>
                <td>{{ artist.style }}</td>
            </tr>
            <tr>
                <th>Bio</th>
                <td>{{ artist.bio }}</td>
            </tr>
            <tr>
                <th>Abonnés</th>
                    <td>
                        {% for subby in artist.subscribers %}
                            <p>{{ subby.firstName }}</p>
                        {% endfor %}
                    </td>
            </tr>
            <tr>
                {{ form_start(subForm) }}
                    {{ form_widget(subForm) }}
                {{ form_end(subForm) }}
            </tr>
        </tbody>
    </table>
    <div>
{#{{  dump(artist.subscribers[219]) }}#}
{#        {{  dump(user) }}#}
{#        {{  dump(artist.news[0].content) }}#}
    </div>

    <a href="{{ path('artist_index') }}">back to list</a>

    <a href="{{ path('artist_edit', {'id': artist.id}) }}">edit</a>

    {{ include('artist/_delete_form.html.twig') }}
{% endblock %}
